/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Comment out or remove the following lines for development
  // output: 'export',
  // images: {
  //   unoptimized: true,
  // },
  // assetPrefix: '/casefox.in/',
  // basePath: '/casefox.in',
}

module.exports = nextConfig